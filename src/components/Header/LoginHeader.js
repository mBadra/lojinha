import React from "react";
import { NavLink } from "react-router-dom";

export default function LoginHeader() {
  return (
    <div>
      <NavLink to="/Login">Login Header</NavLink>
    </div>
  );
}
